# Packages

Publishable or distribution-specific packages that belong to the coding-tools-mcp release ecosystem live here.

- `npm-launcher/`: the thin npm launcher that resolves and starts the Python `coding-tools-mcp` package.

The core Python package remains at `coding_tools_mcp/` to preserve its established import and packaging layout.
